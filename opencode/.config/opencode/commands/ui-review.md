---
description: Visual / UI review of components, layouts, and design tokens.
agent: reviewer
model: fireworks-ai/accounts/fireworks/models/glm-5p1
---
Use the frontend-design skill.

Review the requested UI surface for visual hierarchy, spacing, accessibility,
state coverage (loading, empty, error), responsiveness, and consistency with
existing design tokens. Findings first.
