---
description: Draft a client proposal from requirements, notes, or sample references using the user's proposal style.
agent: docs-writer
---

Use the `proposal-writing` skill.

Draft a client proposal or structured proposal draft from the supplied requirement, notes, or referenced files.
Read `~/OPINIONS.md` and `~/VOICE.md` before drafting if available.
Do not create PDF/DOCX files unless explicitly requested.
When DOCX is explicitly requested, use `~/.config/opencode/skills/proposal-writing/scripts/build_docx.py` so the sanitized sample proposal structure, header, footer, styles, and sender logo are reused consistently.

Request:
$ARGUMENTS
